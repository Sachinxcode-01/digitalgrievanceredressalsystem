---
phase: 8
plan: 1
wave: 1
gap_closure: false
---

# Plan 8.1: Production Hardening & CI/CD Pipeline

## Objective
Establish a robust build process and CI/CD strategy to support a public institutional launch. This ensures every commit is verified before hitting the live server.

## Context
- `package.json`: Review build scripts.
- `vite.config.mjs`: Build optimization settings.
- `server/index.js`: Production serving logic.

## Tasks

<task type="auto">
  <name>Build Hardening</name>
  <action>
    1. Optimize Vite build for code splitting.
    2. Ensure environment variables are strictly types (VITE_ prefix).
    3. Add a post-build verification script to ensure 'dist/index.html' exists and is valid.
  </action>
</task>

<task type="auto">
  <name>Process Management</name>
  <action>
    1. Add 'pm2' or similar process management configuration for Node.js production.
    2. Define health check endpoints for cloud monitoring.
  </action>
</task>

<task type="checkpoint:decision">
  <name>Deployment Provider Setup</name>
  <action>
    Discuss with User:
    - Render.com vs Vercel (Current stack supports standard Node apps).
    - Setting up GitHub Actions for automated deploys.
  </action>
</task>

## Success Criteria
- [ ] 'npm run build' completes without warnings in < 30s.
- [ ] Build artifact correctly serves from the Express backend via 'npm start'.
- [ ] CI/CD plan documented for deployment automation.
