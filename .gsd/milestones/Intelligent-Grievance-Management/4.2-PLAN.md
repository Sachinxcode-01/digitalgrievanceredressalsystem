---
phase: 4
plan: 2
wave: 2
---

# Plan 4.2: Bulk Administration & CSV Analytics

## Objective
Provide admins with tools to perform bulk actions and export analytics data. High volume systems require efficient list management rather than 1-by-1 clicks.

## Context
- .gsd/SPEC.md
- src/pages/AdminDashboard.jsx
- src/api/grievanceService.js

## Tasks

<task type="auto">
  <name>CSV Export Functionality</name>
  <files>
    src/pages/AdminDashboard.jsx
  </files>
  <action>
    1. Add a "Download CSV" button to the Admin Dashboard header/controls area.
    2. Write a utility function that converts the currently fetched array of grievances into CSV format (handling commas in descriptions securely).
    3. Trigger a browser file download natively without needing a backend route.
  </action>
  <verify>
    Click "Download CSV" in the UI.
  </verify>
  <done>
    A properly formatted .csv string is downloaded to the local machine representing the tickets on the dashboard.
  </done>
</task>

<task type="auto">
  <name>Bulk Selection & Update</name>
  <files>
    src/pages/AdminDashboard.jsx
  </files>
  <action>
    1. Introduce a checkbox on each grievance card/row.
    2. Create a floating action bar that appears when `selectedTickets.length > 0`.
    3. Add a "Mark Selected as Resolved" button that iterating/batch updates the Supabase records to 'resolved'.
  </action>
  <verify>
    Select multiple pending tickets and hit "Resolve All".
  </verify>
  <done>
    Supabase successfully processes bulk updates and realtime channel refreshes the UI.
  </done>
</task>

## Success Criteria
- [ ] The downloaded CSV securely handles multi-line descriptions and weird characters without breaking columns.
- [ ] Resolving 5+ tickets at once is fast and visually clear on the UI.
